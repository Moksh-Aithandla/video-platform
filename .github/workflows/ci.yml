name: Build and Push Docker Images

on:
  push:
    branches:
      - vm

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set Short Commit SHA
        run: |
          echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Build and Push Upload API Image
        uses: docker/build-push-action@v6
        with:
          context: ./services/upload-api
          push: true
          tags: |
            mokshaithandla/upload-api:latest
            mokshaithandla/upload-api:${{ env.SHORT_SHA }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan Upload API Image
        uses: aquasecurity/trivy-action@0.36.0
        with:
          image-ref: mokshaithandla/upload-api:${{ env.SHORT_SHA }}
          format: table
          exit-code: 1
          ignore-unfixed: true
          severity: HIGH,CRITICAL

      - name: Build and Push Worker Image
        uses: docker/build-push-action@v6
        with:
          context: ./services/worker
          push: true
          tags: |
            mokshaithandla/worker:latest
            mokshaithandla/worker:${{ env.SHORT_SHA }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Scan Worker Image
        uses: aquasecurity/trivy-action@0.36.0
        with:
          image-ref: mokshaithandla/worker:${{ env.SHORT_SHA }}
          format: table
          exit-code: 1
          ignore-unfixed: true
          severity: HIGH,CRITICAL 