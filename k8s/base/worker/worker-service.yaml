apiVersion: v1
kind: Service

metadata:
  name: worker
  namespace: video-platform
  labels:
    app: worker

spec:
  selector:
    app: worker

  ports:
    - name: http
      port: 3001
      targetPort: 3001